# Phase 5 — Integration dogfood (preflight only)

**Plan:** `~/.claude/skills/2026-04-21-skills-updates-design.md`
**Date:** 2026-04-21
**Goal:** Run the full 4-skill core loop (plus an exercise of the `/setup-worktree` side-car) on a real repo to catch anything per-phase verification missed.
**Addresses:** GAP-01, GAP-02, GAP-03, GAP-04, GAP-05 (end-to-end verification).

## Executed

Preflight only:

- Pre-phase tarball snapshot: `~/.claude/skills.pre-phase-5.tgz` created.
- All 6 SKILL.md files verified loaded by the harness (visible in skill catalog) and strict-YAML parseable.
- Dependency check: `git`, `python3`+`yaml`, and tooling to run `gh pr view` / `gh pr edit` (if team-flow exercise is chosen) should be available in the target repo. Confirm at Task 1 resolution.

## Pending human

### Task 1 — Pick a small, real target repo with low-stakes pending changes `[human]`

**Blocking.** The dogfood needs a real repo — `/execute-phase` creates branches and commits, so the target must be a working git repo where:

- You're comfortable letting the chain create `refactor/phase-<N>-<slug>` branches.
- Tests are reasonable to run (the audit's test-status check is part of the flow).
- There's real work worth planning (not a stale fixture).
- Stakes are low enough that a verification fail or scope violation is educational, not production-breaking.

The skills dir itself (`~/.claude/skills/`) is **not** a good target — it's not under version control, so `/execute-phase` would abort at its first preflight check (working-tree clean requirement). A proper git repo is required.

**Candidates you might have on disk** (just suggestions — you know your work better than I do):

- A small personal tool or CLI — auditable in under a minute, safe to branch on.
- A plugin/skill/agent repo you're already iterating on.
- A scratch repo you've been meaning to refactor.

**What to answer:**

1. Absolute path to the target repo (e.g. `/Users/alexwelch/projects/some-cli`).
2. Current branch state (clean? any uncommitted work? any open PRs?).
3. `/repo-audit` scope — full repo or a `path=<subtree>`?
4. `/design-plan` outcome hint — one sentence on what "done" looks like, so the plan can be realistic rather than abstract.
5. Whether to exercise the `gh pr edit` apply path in `/describe-pr` (requires a real PR to update) or stay in text-only mode.

Once resolved, the chain proceeds:

- Task 2 `[auto]`: `/repo-audit` on the target.
- Task 3 `[auto]`: `/design-plan` on the resulting audit.
- Task 4 `[auto]`: `/execute-phase phase=0` with auto-proceed.
- Tasks 5–9: scope-isolation verification, human review, optional worktree exercise, `/describe-pr`, `/post-mortem scope=partial`.

Chain halts here. Cannot advance without a concrete target repo.

## Verification (Phase 5 preflight gate)

- [x] Pre-phase tarball created.
- [x] All 6 SKILL.md files loaded in harness (confirmed by skill catalog visibility).
- [ ] Target repo selected (Task 1).
- [ ] All subsequent Phase 5 tasks pending.

## Rollback (reference)

If Phase 5 is abandoned before Task 1 resolves: no action needed — no new artifacts created. The core-loop skills remain usable for future sessions.

If Phase 5 starts but the dogfood run surfaces a blocker in one of the new skills: use the appropriate pre-phase tarball to roll back just that skill's changes (`tar -xzf ~/.claude/skills.pre-phase-<N>.tgz -C ~/.claude --include='skills/<skill>/*'`). Dogfood phase branches in the target repo: `git branch -D refactor/phase-*` and `git worktree remove ~/wt/<repo>/phase-*/` as needed.

## Commits

None — Phase 5 preflight has no code changes yet. Snapshot: `~/.claude/skills.pre-phase-5.tgz`.

## Scope violations

None. Preflight is backup + halt only.

## Follow-ups

Carried forward from Phase 4 — all will be naturally exercised or surfaced by the dogfood:

- **NEW-06** — `/execute-phase dry_run=true` path will fire at some point during the dogfood (either as an explicit probe at Task 4, or during any verification pass). Plan to close.
- **NEW-07** — cluster-grouping heuristic in `/execute-phase` Step 2 — watch whether the audit's plan forces serial execution where parallel would have worked.
- **NEW-08** — duplicated branch-slug derivation — will be observable during Task 7 if `/setup-worktree` gets exercised.
- **NEW-09** — `/describe-pr` skip-vs-miss distinction — surfaces at Task 8 if any plan phase is skipped.

## Chain state

- Phase 0: done
- Phase 1: done
- Phase 2: done
- Phase 3: done
- Phase 4: done
- Phase 5: preflight only — halted at Task 1 `[human]` (target-repo selection)

## Auto-proceed decision

Preflight PASS. Pending human: Task 1 (target-repo selection).

→ **HALT.** Cannot run `/repo-audit` against an unspecified target.
