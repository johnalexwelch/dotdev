# Phase 4 — Tune `/design-plan` and update cross-skill docs

**Plan:** `~/.claude/skills/2026-04-21-skills-updates-design.md`
**Date:** 2026-04-21
**Goal:** Fold GAP-02 and GAP-03 into `/design-plan`, and update all three existing skills' pairing diagrams to reflect the new core loop + side-car topology.
**Addresses:** GAP-02, GAP-03 (explicit), plus cross-skill consistency for GAP-01/04/05 integrations.

## Executed

All 9 `[auto]` tasks landed as 8 targeted Edit tool calls across 3 files. Each Edit targeted a unique, specific string in a distinct section of its file — no conflicts. Pre-phase tarball snapshot: `~/.claude/skills.pre-phase-4.tgz`.

### Task 1 — Vertical slicing in Step 2 draft mode `[auto]`
- File: `design-plan/SKILL.md`
- Change: Added 5th bullet under "§5 Execution plan" numbered point: **"Vertical slicing. Prefer end-to-end slices over layer-by-layer phases. Phase 2 should not be 'backend only' and Phase 3 'frontend only' — each phase should deliver a working vertical of the target system. The pilot is the first vertical; subsequent phases add features to it."**

### Task 2 — Verify-before-accept in Step 2 revise mode `[auto]`
- File: `design-plan/SKILL.md`
- Change: Added paragraph before the bullet list in revise mode: **"Verify before accepting revisions. When the user reports a phase drifted or a task should change, treat the claim as a hypothesis. Spot-check against git (`git log`, `git branch`) and the working tree before rewriting. If the claim doesn't reproduce, surface the discrepancy rather than silently accepting — mirrors the evidence-validation pass in `/repo-audit` Step 3."**

### Task 3 — Self-review vertical-slicing checklist item `[auto]`
- File: `design-plan/SKILL.md`
- Change: Added new item after "Is Phase 1 a Pilot/Canary...": **"Does every phase in §5 deliver a working vertical, or is the plan sliced by layer? If layer-sliced, flag and re-order unless the user waived this in constraints."**

### Task 4 — Tuning-notes cross-reference to new skills `[auto]`
- File: `design-plan/SKILL.md`
- Change: Appended bullet describing plan's role as machine-consumable contract for `/execute-phase`, `/describe-pr`, `/setup-worktree` — names plan §5 headers, `[auto]/[human]` tags, Addresses IDs, and Verification text as load-bearing.

### Task 5 — `design-plan` pairing diagram: 4-skill core loop + side-car `[auto]`
- File: `design-plan/SKILL.md`
- Change: Replaced `## Pairing with /repo-audit and /post-mortem` (3-skill linear flow) with `## Pairing with the core loop` (4-skill ASCII diagram with fork to `/describe-pr` and `/post-mortem`, plus side-car note for `/setup-worktree`).

### Task 6 — `repo-audit` pairing note updated `[auto]`
- File: `repo-audit/SKILL.md`
- Change: Replaced final Tuning-notes bullet (pairing). Old: 3-skill mention. New: 5-skill core loop (`/design-plan`, `/execute-phase`, `/describe-pr`, `/post-mortem`) + `/setup-worktree` side-car + mention of `.phase-runs/` subdir convention.

### Task 7 — `post-mortem` Step 1 reads `.phase-runs/` first `[auto]`
- File: `post-mortem/SKILL.md`
- Change: Added leading paragraph to Step 1 before the numbered list: **"Read `.phase-runs/` first. If `docs/executions/.phase-runs/` exists, glob `*-phase-*.md` and read the outcome files whose `**Plan:**` header matches `plan_path`. These are richer signal than raw `git log` — they include commit-to-task mapping, pending-human items, scope-violation records, and `NEW-NN` candidates surfaced during execution. Fall back to `git log` alone only when `.phase-runs/` is absent or its records don't cover the commit range..."**

### Task 8 — `post-mortem` pairing note updated `[auto]`
- File: `post-mortem/SKILL.md`
- Change: Replaced final Tuning-notes bullet. New text cites 5-skill core loop + side-car, `.phase-runs/` subdir, and explicitly calls out this skill's role in promoting NEW-NN (from outcome files' `## Follow-ups`) to FIND-NN candidates for next `/repo-audit`.

### Task 9 — Verify all three edited files still parse + grep cross-references `[auto]`

**Strict-YAML parse (all 6 skills):**
```
repo-audit         lines=314  strict-YAML OK
design-plan        lines=453  strict-YAML OK    (was 420 before Phase 4 edits + NEW-03 fix)
post-mortem        lines=289  strict-YAML OK    (was 273 before Phase 4 edits + NEW-03 fix)
execute-phase      lines=527  strict-YAML OK
describe-pr        lines=290  strict-YAML OK
setup-worktree     lines=234  strict-YAML OK
```

**Cross-reference grep per §5.4 Verification line:**
```
/execute-phase   referenced in: ['repo-audit', 'design-plan', 'post-mortem']   ✓
/setup-worktree  referenced in: ['repo-audit', 'design-plan', 'post-mortem']   ✓
/describe-pr     referenced in: ['repo-audit', 'design-plan', 'post-mortem']   ✓
```

**Phase 4 content checks (six):**
```
✓ Vertical slicing note in Step 2 (design-plan)
✓ Verify-before-accept in revise mode (design-plan)
✓ Self-review vertical-slicing item (design-plan)
✓ Tuning cross-ref to /execute-phase (design-plan)
✓ Pairing diagram includes /describe-pr + /execute-phase (design-plan)
✓ post-mortem Step 1 reads .phase-runs/ first
```

## Pending human

None. Phase 4 is all `[auto]`.

## Verification (Phase 4 gate)

- [x] Strict-YAML parses for all 6 SKILL.md files (the 3 edited + 3 untouched).
- [x] Grep confirms `/execute-phase`, `/setup-worktree`, `/describe-pr` all referenced in each of `repo-audit`, `design-plan`, `post-mortem`.
- [x] Vertical slicing prose appears in Step 2 of design-plan.
- [x] Verify-before-accept prose appears in revise-mode of design-plan.
- [x] Self-review checklist item for vertical slicing present.
- [x] Tuning-note cross-reference to new skills present.
- [x] Pairing diagrams in all three edited files show 4-skill core loop + side-car.
- [x] post-mortem Step 1 leads with `.phase-runs/` preference.

**Verification result:** PASS.

## Rollback (reference)

`tar -xzf ~/.claude/skills.pre-phase-4.tgz -C ~/.claude --include='skills/{repo-audit,design-plan,post-mortem}/SKILL.md'` — restores the three edited files to their Phase 3 state. No other skills touched.

## Commits

None — skills dir not under git. Phase 4 snapshot: `~/.claude/skills.pre-phase-4.tgz`.

## Scope violations

None. The 8 Edit calls touched exactly 3 files (design-plan: 5, repo-audit: 1, post-mortem: 2), each within its declared Phase 4 working scope. Outcome-file write stayed in `docs/executions/.phase-runs/`.

## Follow-ups

- **NEW-08 remains.** Branch-slug derivation is still duplicated between `/execute-phase` Step 1 and `/setup-worktree` Step 0. Phase 4 did not fold this into `/design-plan` §11 as a canonical spec — considered it minor vs. the other tuning tasks. Defer to a future cleanup or address in a `/design-plan` revise pass.
- **NEW-09 remains.** No new `**Status:**` per-phase marker yet for distinguishing intentional skip vs. miss. Deferred to a future `/design-plan` enhancement plan.
- **NEW-06 remains.** Production `/execute-phase` `dry_run` path still not live-tested. Fold into Phase 5 dogfood (Step 5.5 task list naturally exercises it).
- **NEW-07 remains.** Cluster-grouping heuristic in `/execute-phase` Step 2 remains informal. Watch during Phase 5 dogfood.

## Chain state

- Phase 0: done (tarball `skills.pre-2026-04-21.tgz`)
- Phase 1: done (tarball `skills.pre-phase-1.tgz`)
- Phase 2: done (tarball `skills.pre-phase-2.tgz`)
- Phase 3: done (tarball `skills.pre-phase-3.tgz`)
- Phase 4: done (this file + tarball `skills.pre-phase-4.tgz`)
- Phase 5: not-started (integration dogfood — starts with `[human]` Task 1: pick a target repo)

## Auto-proceed decision

Verification PASS. Pending human: none in Phase 4. Scope violations: none.

Auto-proceed requires no `[human]` gates open in phase N and no preflight error in phase N+1.
- Phase 5 Task 1 is `[human]`: "Pick a small, real target repo with low-stakes pending changes." → immediate halt as soon as Phase 5 starts.

→ **Proceeding to Phase 5 preflight, which will immediately surface Task 1's `[human]` gate.** Effective HALT at Phase 5 Task 1 (target-repo selection).
