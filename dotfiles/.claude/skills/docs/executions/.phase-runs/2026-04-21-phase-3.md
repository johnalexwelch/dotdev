# Phase 3 — Port `/describe-pr` and `/setup-worktree`

**Plan:** `~/.claude/skills/2026-04-21-skills-updates-design.md`
**Date:** 2026-04-21
**Goal:** Port the two supporting Desktop skills, adapted to globals' conventions. Both are smaller surface-area than `/execute-phase`, so they bundle into one phase.
**Addresses:** GAP-04 (`/describe-pr`), GAP-05 (`/setup-worktree`).

## Executed

Both ports were independent (disjoint file scopes) and dispatched in parallel per §7 scope-based isolation — two Write tool calls in the same message, each scoped to exactly one new file. Pre-phase tarball snapshot: `~/.claude/skills.pre-phase-3.tgz`.

### Tasks 1–7 — Port `/describe-pr` `[auto]`

- **File:** `~/.claude/skills/describe-pr/SKILL.md` (290 lines).
- **Task 1 — Source read.** Desktop `describe-pr/SKILL.md` already cataloged in Phase 0; port-adaptation surfaces reused.
- **Task 2 — Inlined script.** `scripts/pr_diff_urls.sh` replaced with inline Step 1.4: `git remote get-url origin` → parse `github.com:<owner>/<repo>` → `git hash-object` for per-file anchors → construct PR permalinks. When no PR exists, fall back to `compare/<base>...<branch>#diff-<anchor>` URLs.
- **Task 3 — Subagent adapted.** `subagent_type=implementation-reviewer` → `general-purpose` with an explicit deviation-analysis brief: per-phase status (as planned | drifted | skipped | not this PR), verbatim task text for drifted phases, commit-hash citations, `## Scope violations` / `## Follow-ups` elevation to risk section.
- **Task 4 — Vocabulary.** Dropped Linear-specific `linear get-issue-v2` call and hardcoded Linear URL assembly. Ticket-reference detection is now pluggable regex (`FIND-NN`, `NEW-NN`, `GAP-NN`, `phase-N`, JIRA-style `[A-Z]+-\d+`). Ticket URLs are opt-in via a repo-root `.tickets.env` file (`TICKET_BASE_URL=...`) — no default Linear assumption.
- **Task 5 — `.phase-runs/` integration.** Step 1.2 globs `docs/executions/.phase-runs/*-phase-*.md` and filters to branches in the PR range. Phase-run outcome files are the preferred evidence source; `git log` is the fallback. Novel addition vs. Desktop source: `## Phases completed` section maps commits to plan §5 phase numbers.
- **Task 6 — Frontmatter.** 5 inputs (`plan_path`, `pr_number`, `branch`, `apply`, `base`), 4 triggers, `reads`/`writes` per §6. `name`, `description`, `triggers`, `persona` all present.
- **Task 7 — §6 structure.** Body: Purpose → Step 0 Preflight → Step 1 Gather inputs → Step 2 Dispatch → Step 3 Compose → Step 4 Apply → Step 5 Surface → Output Format → Error Handling (9 rows) → Example Invocation (2 blocks: PR-exists + no-PR) → Tuning notes (5 bullets) → Pairing diagram. 18 body sections total.

### Tasks 8–13 — Port `/setup-worktree` `[auto]`

- **File:** `~/.claude/skills/setup-worktree/SKILL.md` (234 lines).
- **Task 8 — Source read.** Desktop `setup-worktree/SKILL.md` already cataloged in Phase 0.
- **Task 9 — Inlined script.** `scripts/create_worktree.sh` script-first-with-fallback logic flattened. Step 1 is unconditionally inline: `git worktree add "<path>" "<branch>"` (existing branch) or `git worktree add -b "<branch>" "<path>"` (new branch). No external-script conditional.
- **Task 10 — Expanded env detection.** Source's `.env*` glob expanded to: `.env`, `.env.local`, `.env.development/production/test`, `.envrc`, `.nvmrc`, `.python-version`, `.tool-versions`, `.ruby-version`, `.node-version`, `.claude/settings.local.json`. Best-effort copy with silent skip for missing parent dirs.
- **Task 11 — `/design-plan` integration.** Accepts `plan_path` + `phase` inputs. When both set, derives branch name from §5.<N> phase header using the **same slug derivation `/execute-phase` uses** (lowercase, non-alphanum → `-`, trim, cap 40 chars) — guarantees the branch name matches what `/execute-phase` would create. Default path: `~/wt/<repo>/phase-<N>/`. Standalone skill — explicitly not inlined by `/execute-phase`.
- **Task 12 — Frontmatter.** 5 inputs (`plan_path`, `phase`, `branch`, `path`, `setup_command`), 4 triggers, `reads`/`writes` per §6.
- **Task 13 — §6 structure.** Body: Purpose → Step 0 Preflight → Step 1 Create worktree → Step 2 Copy env → Step 3 Setup command → Step 4 Surface → Output Format → Error Handling (10 rows) → Example Invocation (2 blocks: from-halt + explicit) → Tuning notes (7 bullets) → Pairing note (side-car, not core-loop). 8 body sections (tighter because this is a thin shell around `git worktree add`).

**Strict-YAML verification:**
```
describe-pr        lines=290  inputs=5  triggers=4  sections=18   strict-YAML OK
setup-worktree     lines=234  inputs=5  triggers=4  sections=8    strict-YAML OK
```

**Harness confirmation:** Both skills visible in the in-session skill catalog with their production descriptions — the skill loader picked them up immediately.

## Pending human

### Task 14 — Review both SKILL.md files against §6 Authoring standard `[human]`

Per plan §5.3 Task 14. User must review against the **§6 Authoring standard checklist**:

1. **One `SKILL.md` per skill.** No `references/` subfolder. ✅ Both.
2. **Frontmatter keys:** `name`, `description`, `triggers`, `persona`, `inputs`, `reads`, `writes`. ✅ Both.
3. **Body section order:** `# <slug> — <title>` → Purpose → Step 0 Preflight → Step 1..N → Output Format → Error Handling (markdown table) → Example Invocation (fenced) → Tuning notes. ✅ Both (with Pairing diagrams appended below Tuning, as the existing three globals also do).
4. **Templates inline in fenced blocks.** ✅ `describe-pr` inlines the PR body template (was `{SKILLBASE}/references/pr_description_template.md`). `setup-worktree` inlines the chat-summary format.
5. **Voice:** direct, imperative, no hedging. ✅ Spot-check any step text.
6. **Every procedural step that consumes another skill's artifact names that artifact by path pattern.** ✅ `describe-pr` names `docs/executions/.phase-runs/<date>[-<plan-slug>]-phase-<N>.md` and `docs/plans/<date>-design.md`. `setup-worktree` names `docs/plans/<date>-design.md` when deriving from plan+phase.
7. **No Desktop-specific subagent types** (`implementer-agent`, `implementation-reviewer`). ✅ `describe-pr` uses `general-purpose`. `setup-worktree` uses no subagents (direct bash).
8. **No external script dependencies.** ✅ `pr_diff_urls.sh` and `create_worktree.sh` both inlined.
9. **Globals' ID vocabulary** (`FIND-NN`, `NEW-NN`, phase numbers — not Desktop ticket slugs or `.humanlayer/tasks/`). ✅ Both.

**Skill-specific review items:**

**`/describe-pr`:**
- The pluggable-ticket-URL design (`.tickets.env` opt-in) — does this match your actual ticket-linking preferences, or would you rather hardcode a default per-repo convention?
- `## Phases completed` + `## New findings surfaced during execution` sections are novel vs. Desktop — do they feel useful or do they bloat the PR body? Can be shortened in Tuning notes later.
- When `.phase-runs/` files are absent (e.g., PR predates `/execute-phase`), the skill falls back to raw `git log` and notes the degradation. Is that graceful enough, or should it refuse to produce a body?

**`/setup-worktree`:**
- Env-file copy list — any project-specific files you routinely need (e.g., `.prettierrc.local`, `.tool-versions.local`) that should be in the default set?
- Default path `~/wt/<repo>/phase-<N>/` — any conflict with existing `~/wt/` conventions you use?
- Branch-slug derivation matches `/execute-phase` exactly (same lowercase-nonalphanum-trim logic). Confirmed by cross-reference in the source skill. If `/execute-phase`'s slug logic changes, this skill needs to change in lockstep — worth a Phase 4 note or just trust review.

Chain halts here. No Phase 4 work until this gate is resolved.

## Verification (Phase 3 gate)

- [x] `describe-pr/SKILL.md` exists (290 lines) and strict-YAML parses.
- [x] `setup-worktree/SKILL.md` exists (234 lines) and strict-YAML parses.
- [x] Both files follow §6 Authoring standard (frontmatter keys, body section order, inline templates, no `references/`, no Desktop subagent types, no external scripts, globals' ID vocabulary).
- [x] `describe-pr` integrates with `.phase-runs/` (Step 1.2 reads them as preferred evidence; Step 3's template has `## Phases completed` section).
- [x] `setup-worktree` integrates with `/design-plan` (Step 0 derives branch + path from `plan_path` + `phase`).
- [x] Harness skill catalog shows both skills with production descriptions.
- [ ] **Human sign-off (Task 14)** — per §6 checklist + skill-specific items above.
- [ ] **Live verification deferred** — §5.3 Verification calls for running `/setup-worktree plan_path=<plan> phase=2` to confirm worktree creation + env-file copy, and `/describe-pr pr_number=<test>` to confirm PR body output. Neither run in Phase 3; both are cheap enough to include in Phase 5 dogfood.

**Verification result:** PASS pending human review.

## Rollback (reference)

- Remove both: `rm -rf ~/.claude/skills/describe-pr/ ~/.claude/skills/setup-worktree/`
- Restore from tarball: `tar -xzf ~/.claude/skills.pre-phase-3.tgz -C ~/.claude`
- No other skills touched (scope-disjoint writes confirmed — each parallel Write landed in its own new directory).

## Commits

None — skills dir not under git. Phase 3 snapshot: `~/.claude/skills.pre-phase-3.tgz`.

## Scope violations

None. Each of the two parallel Writes touched exactly one new file (new directory, new `SKILL.md`). No cross-touching. Outcome file write stays within declared `docs/executions/.phase-runs/` scope.

## Follow-ups

- **NEW-08 (low) — Shared branch-slug derivation logic.** `/execute-phase` Step 1 and `/setup-worktree` Step 0 both derive a phase slug from a plan's `### §5.<N> Phase <N> — <text>` header using the same rule (lowercase, non-alphanum → `-`, trim, cap 40). Currently duplicated prose in both files. If/when this rule ever changes, both skills need updating in lockstep. **Resolution candidate:** a short note in `/design-plan` §11 specifying the slug rule as the canonical spec; both skills reference that spec. Can fold into Phase 4 Task 1 or defer.

- **NEW-09 (low) — `/describe-pr` can't distinguish "phase skipped intentionally" from "phase accidentally missed"** beyond what the plan's §9 Open questions says. If the plan doesn't explicitly defer a phase, a skip looks like a miss. **Resolution candidate:** `/design-plan` could grow a `**Status:**` line per phase in revise mode (which it already does); `/describe-pr` could read that. Defer to Phase 4 or later; not a blocker.

- **Live-verification deferred to Phase 5.** Both new skills have `## Example Invocation` blocks showing expected output, but neither has been run against a real plan + PR / real repo. Phase 5 dogfood is the natural place to close.

## Chain state

- Phase 0: done (tarball `skills.pre-2026-04-21.tgz`)
- Phase 1: done (tarball `skills.pre-phase-1.tgz`)
- Phase 2: done (tarball `skills.pre-phase-2.tgz`)
- Phase 3: done pending Task 14 review (this file + tarball `skills.pre-phase-3.tgz`)
- Phase 4: not-started (tune `/design-plan` + cross-skill docs)
- Phase 5: not-started (integration dogfood)

## Auto-proceed decision

Verification PASS. Pending human: Task 14.

→ **HALT.** Do not advance to Phase 4 until Alex reviews Task 14 gate.
