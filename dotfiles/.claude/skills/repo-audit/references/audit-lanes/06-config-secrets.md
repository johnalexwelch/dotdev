# 06 — Config Secrets

Question:

```markdown
Anything committed that shouldn't be (tokens, keys, .env)? Machine-specific paths hardcoded? Secrets referenced vs. embedded? Check `.env.example` drift.
```
