# 01 — Built Vs Planned

Question:

```markdown
What's built vs. what's planned. Read every planning/spec doc at root and compare to actual directory contents in `<path>`. List the delta — planned but missing, built but undocumented, phases incomplete.
```
