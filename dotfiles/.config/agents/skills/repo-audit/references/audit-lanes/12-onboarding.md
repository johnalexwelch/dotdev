# 12 — Onboarding

Question:

```markdown
If someone new cloned this repo tomorrow, how long until they could run the core workflow? What's undocumented but load-bearing — hardcoded paths, unwritten norms, tribal knowledge, required machine-local state?
```
