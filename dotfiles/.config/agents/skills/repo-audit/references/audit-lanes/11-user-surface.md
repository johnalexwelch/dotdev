# 11 — User Surface

Question:

```markdown
Full list of user-facing commands/UIs. Do they behave consistently on errors? Is the voice/style coherent across them? What does a user see when something fails?
```
