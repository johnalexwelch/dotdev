# 10 — Security Depth

Question:

```markdown
Beyond config-secrets: PII in logs or artifacts, token scope minimization, dependency pinning and known vulns, data retention policy, permissions model. Check git history for previously-committed secrets (`git log -p -- '*.env'` and similar).
```
