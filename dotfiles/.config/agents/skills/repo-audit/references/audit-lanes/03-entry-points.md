# 03 — Entry Points

Question:

```markdown
What's a user actually meant to run? Which entry points work, which are stubs, which are abandoned? Include slash commands, CLIs, scripts, cron/launchd jobs.
```
