---
name: workflow-build-one
disable-model-invocation: true
description: Superseded by workflow-deliver (D-006 #11, 2026-08-18). Do not invoke; Load and run workflow-deliver with the matching kind instead.
---

# Workflow Build One — superseded

Merged into `workflow-deliver` on 2026-08-18 (D-006 #11): one orchestrator, kind-templated ledger gates. For any work that would have routed here, Load and run `workflow-deliver/SKILL.md` with `kind=feature` (or `skill`/`docs` as the item warrants). This directory remains only until Phase 4/5 rewires remaining callers; do not add content here.
