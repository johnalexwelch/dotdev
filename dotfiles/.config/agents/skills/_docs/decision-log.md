# Skills Corpus — Decision Log

Accepted decisions for the personal skills corpus (`~/dotdev/dotfiles/.config/agents/skills`). Append-only; treat entries as settled context unless explicitly reopened.

## D-001 — Extract `review-scaffolding`; reviews become thin adapters (2026-06-01)

**Decision.** Introduce a `review-scaffolding` library (`user-invocable: false`, `disable-model-invocation: true`) holding the shared review **discipline**, **severity vocabulary**, and **report contract**. The single-pass `*-review` skills become thin adapters that declare their own criteria/lens, unit of analysis, persistence, and deltas, and defer to the scaffold for everything else. Mirrors the existing `council-scaffolding` pattern.

**Scope (refined during rollout — 5 analytical adapters).** `clarity-review`, `pacing-review`, `sql-review`, `dashboard-review`, `metric-tree-review`. The dnd trio (`dnd-review`, `dnd-open-thread-review`, `dnd-player-agency-review`) was split out after reading them — see D-002.

**Excluded, with reasons.** `review`, `workflow-review`, `strategic-analysis-review` dispatch subagents → fan-out/gate family, not single-pass review (belong with the council/dispatch pattern). `receive-review` is a different verb (processing incoming PR feedback, not assessing an artifact).

**Alternatives considered.** (b) lighter "link a shared reference, no rigid migration" and (c) "leave reviews independent" were considered and rejected in favor of (a) full scaffold for cross-review consistency. Honest tradeoff accepted: unlike councils (which share a dispatch *engine*), reviews share only a *contract* — the win is consistency + one place to evolve the discipline, not shared machinery.

**Verification.** Per-adapter: criteria preserved verbatim; report shape + discipline sourced from the scaffold; a golden artifact + expected-report pair shows the review output materially unchanged before/after migration.

**Migration / rollback.** Vertical slice: migrate one review first (`clarity-review` — no graph, no persist), confirm unchanged, then the remaining 7. Rollback = revert the single adapter SKILL.md; reviews are independent, blast radius is one skill.

**Second pass.** `not_needed` — the scaffold is one reference doc; no internal submodule earns its own seam.

**Status.** Done: `review-scaffolding` created; `clarity-review` (deep thin-out — it carried the most generic boilerplate), then `sql-review`, `dashboard-review`, `metric-tree-review`, `pacing-review` (lighter touch — already domain-specific, so migration = declare adapter + defer discipline/severity/report-skeleton, keep domain criteria/output/gates). All verified.

**Open follow-up.** Host activation/resolution (linking `review-scaffolding` + adapters into `~/.claude/skills` so the deferral resolves at runtime) is the separate sub-skill-invocation-seam question (improve-codebase-architecture candidate 3), deferred.

## D-002 — dnd review trio held out of `review-scaffolding`; needs its own consolidation decision (2026-06-01)

**Decision.** The three dnd reviews are **not** migrated onto `review-scaffolding`. Reading them showed a different shape: `dnd-open-thread-review` produces a *thread inventory + status classification + `OPEN_THREADS.md` writeback* (not findings-with-fixes), and the dnd reviews use domain verdicts (Critical/High/Opportunity, thread statuses, "Safe to Run?") rather than the shared `[HIGH]/[MED]/[LOW]` severity. Forcing them under the general scaffold would distort them.

**Separate problem found.** `dnd-review` is a 3-mode skill (continuity / threads / agency) whose modes **overlap the standalone `dnd-open-thread-review`, `dnd-player-agency-review`, and `dnd-continuity-check`**. That intra-dnd duplication is its own consolidation question (one multi-mode skill vs. three standalones, or a `dnd-review-scaffolding`), to be decided separately — not bundled into the review-scaffolding work.

**Status.** Deferred; no dnd files changed.

## D-003 — One sub-skill invocation convention + resolution rule + lint (2026-06-01)

**Decision.** Adopt a single cross-skill invocation form — `Load and run \`<name>/SKILL.md\`` (workflow steps) and `follow \`<name>\``(libraries) — resolving against the **active skills root** (`~/.claude/skills`, canon behind the links). Flow-arrow prose is a map, not an invocation.`lint-skill-refs.sh` guards against hollow references (a linked skill pointing at an unlinked one). Convention documented in `CONVENTIONS.md`.

**Why.** Grounded measurement: only 3 skills used the explicit form; 30 used arrow-only prose that names skills without an invocation or a resolution rule — the gap that made `/workflow-debug` and `/grill-with-docs` hollow earlier this session.

**Status.** Shipped: `CONVENTIONS.md` + `lint-skill-refs.sh`. Lint is **clean** on the active root — the core delivery loop's explicit refs (`workflow-build-one`/`-debug`/`-finalize` → their sub-steps) all resolve, because the 14 closure links added earlier fixed them.

**Deferred (lint-driven).** Converting the ~30 arrow-only orchestrators to explicit refs (expands lint coverage); linking `CONVENTIONS.md` from `write-a-skill`; activating the review adapters (`review-scaffolding` + chosen reviews) into the active root.

## D-004 — Adopt 5 capability upgrades from the MagickPen template review (2026-06-10)

**Decision.** After mapping 173 MagickPen templates against the corpus, adopt the 5 highest-value ideas — but in the *cheapest fit form*, not all as standalone skills:

1. **`review-scaffolding` + tracked-changes** — added an opt-in **tracked-changes output** mechanic to the scaffold (not to one review), so every `*-review` adapter inherits a consistent inline-edit view. Source idea: MagickPen `article-polishing`.
2. **`clarity-review`** — opted into tracked-changes; added **protected elements** (don't edit code/tables/LaTeX) and **readability as an advisory signal only**. Explicitly **rejected** the source template's "strictness / rewrite-aggressiveness" knob — it contradicts clarity-review's stated non-rewrite philosophy.
3. **`bias-auditor` persona + `cognitive-bias-catalog.md`** — built the cognitive-bias capability as an **analysis-council persona + reference**, *not* a standalone skill, to avoid overlap with `decision-scientist`/`counterfactual-check`/`skeptical-data-scientist` and routing ambiguity. Wired into `analysis-council/roster.yml` (optional + dispatch signal + overlay).
4. **`product-launch-checklist`** — new standalone skill (clean whitespace; `workflow-roadmap` plans what to build, nothing covered shipping). Tier-sized (T1/T2/T3), guardrail-first, hands off to `report-metrics` / `post-mortem`.
5. **`okr-generator`** — new standalone skill, decision-first; **delegates KR definition + Goodhart check to `metric-design`** rather than reinventing metric rigor.
6. **`mock-data-generator`** — new standalone utility skill with a working, dependency-light Python generator (FK topological resolve, deterministic seed, dialect-aware SQL) + passing smoke test.

**Alternatives considered & rejected.** Building bias-identifier, NL→formula, commit-message, and several others as standalone skills — rejected to protect the corpus from bloat/routing ambiguity (95→ skills already). Importing the MagickPen *form* (FastAPI/SQLite scaffolding) — rejected; only the rubric/checklist ideas were taken, rendered in the decision-first house style. Legal/compliance and personal-life templates — rejected (liability + out of scope).

**Verification.** `lint-skill-refs.sh` clean for new cross-skill refs; `mock-data-generator` smoke test passes (FK integrity, determinism, cycle-detect, SQL export). Per-skill: SKILL.md under house line budget, description has "Use when" triggers, references one level deep.

**Activation (host-side, pending).** New skills authored in canon. Must be symlinked into the active root (`~/.claude/skills`) and `okr-generator` + `product-launch-checklist` added to `workflow-router` (done in canon) before they resolve at runtime — see apply/activation commands in the session handoff.

**Status.** Authored in canon; activation + git commit pending on host.

## D-005 — Deepen Worktree Baseline into one testable cut/verify/emit interface (2026-07-17)

**Decision.** Extract worktree-gate logic (base-branch resolution, `fetch --prune`, stacked-parent ancestry check, path/branch derivation, env-file copy) behind one Interface with three ops: `cut`, `verify`, `emit` (`WORKFLOW_BASE_GATE` + `WORKTREE_BASELINE_GATE`/`STACKED_WORKTREE_GATE`). Callers invoke it and record what it emits; they stop restating the resolution/derivation procedure.

**Reversal of a prior call.** `setup-worktree/SKILL.md`'s own changelog (2026-05-21 port) deliberately dropped `scripts/create_worktree.sh` for inlined `git worktree add`. This decision reverses that: a real script (`setup-worktree/scripts/worktree-baseline.sh`, `cut`/`verify`/`emit` subcommands) is required because DEEPENING.md's testing bar ("golden gate-block strings at the seam") cannot be met against prose — one adapter (real git) is a hypothetical seam per LANGUAGE.md without a second, local-substitutable adapter (tmp-git-repo test harness, `test/test-worktree-baseline.sh`, same shape as `test/test-workflow-guard.sh`).

**Scope.** 20 callers restate this invariant today (grep-confirmed), not the architecture report's estimated "6+": `setup-worktree`, `execute-phase`, `execute-prd` (+ its `child-execution-brief-template.md`), `prompt-builder`, `run-backlog` (+ `outage-risk-policy.md`, `repo-delivery-policy.md`), `to-issues`, `to-prd`, `triage` (+ `AGENT-BRIEF.md`), `workflow-autonomous-backlog`, `workflow-build-one`, `workflow-debug`, `skill-system-audit`, `workflow-executive-doc`, `workflow-feature`, `workflow-finalize`, `workflow-review`, `workflow-router`. All 20 are the eventual target; only `workflow-build-one` Step 0 is the first vertical slice.

**Vertical slice.** `workflow-build-one` Step 0 calls the script instead of inlining the "Per-Issue Worktree Invariant" section; verify one real issue run still emits a valid gate block. Remaining 19 callers follow after the slice proves the interface, tracked as backlog — not blocked on this decision.

**second_pass.** `not_needed` — cut/verify/emit is one coherent interface, no hidden multi-concept split.

**Alternatives considered & rejected.** Prose-only interface (no script) — rejected because it cannot be tested at the seam, only re-read by an agent, which is what caused the drift this candidate exists to fix.

**Status.** Design accepted (grill complete); no code, script, or SKILL.md edits made yet — implementation pending explicit go-ahead.

## D-006 — Deterministic workflow enforcement: `workflow-ledger` kernel, blocking hooks, corpus restructure (2026-08-19)

**Decision (grilled 2026-08-18/19, HITL, all recommendations accepted; provenance: human).** Move workflow enforcement from prose the model complies with to scripts and hooks the model cannot silently bypass. Full scope:

**1. Substrate first, then chain.** Phase 0 builds the enforcement kernel; chain skills are then migrated in flow order starting at `workflow-router`. Router hardening without the kernel is better prose on the same honor system.

**2. Kernel = new library skill `workflow-ledger`** (dotdev canon; `user-invocable: false`, `disable-model-invocation: true`). Absorbs `_docs/step-ledger.md` + `_docs/state-cockpit.md` (two halves of one protocol) and `_docs/human-gate-taxonomy.md` (gate types become a validated enum: only `reviewer-validation` gates are agent-stampable; `maintainer-decision`/`operator-runtime`/`secret-custody` require `--human` provenance). `scripts/ledger.sh` subcommands: `init`, `set` (validated transitions: required steps unskippable, completed/skipped/blocked require evidence, schema-checked writes), `stamp`, `check`, `show`, `reconcile` (ledger vs git ground truth — closes backlog C20/C32/C15), `preflight` (Requires-field tool checks), `review-floor`, `verify-local` (repo-manifest CI command set run at HEAD — closes C19).

**3. Checked vs attested gate fields.** Every gate field is classified: *checked* fields are computed by the script at stamp time (worktree verify, CI state via gh, resolved review threads, git-clean, freshness, lane artifacts); *attested* fields are recorded verbatim with timestamp (verdicts, rationale). A stamp is writable only when all checked fields pass. Model keeps judgment; loses the ability to report a state that isn't true.

**4. Freshness = strict SHA equality.** `stamp` records `head_sha`; `check` fails on any commit after the stamp, docs-only included. No pathspec exceptions — every exclusion is a future bypass argument. Replaces three prose restatements of review-freshness in `workflow-finalize`.

**5. Override = audited, not prevented.** `stamp --override --reason` writes an `overrides:` entry (reason, timestamp, sha); hooks warn loudly but permit. The model can bypass any control it can execute — the design goal is that bypasses are impossible to do *silently*. Hard-block-only rejected: one script bug would brick delivery and teach hook-disabling.

**6. Blocking hooks** (extend `workflow-guard.sh`): `gh pr merge|ready` blocked (exit 2) without fresh finalize stamp; Edit/Write to `state.yaml` blocked (script-owned; no crypto needed); stderr-suppression (`2>/dev/null`) on mutating `git`/`gh` commands blocked (closes C36). Hooks bite only where `docs/executions/` exists (implicit repo opt-in); work repos untouched.

**7. Router determinism = measurement, not rules engine.** Classification stays model-judged; a golden trigger eval (~40 prompts→expected-route harvested from `skill-invocations.log`, run via `skill-evaluator` trigger-accuracy mode) gates CI on router/description changes. ROUTE_CARD emission gets a schema validator. A keyword rules engine was rejected: deterministically wrong on paraphrase.

**8. Model pinning.** Planning/synthesis tier pinned to Fable (`wayfinder`, `design-plan`, `grill-with-docs`, `v1-system-design`, `workflow-roadmap`); router/execution stay sonnet until eval pass-rate justifies a bump. Verify `model: fable` frontmatter support on one skill before fleet rollout. Wayfinder is the user's high-level planning entry point — judgment layer, Fable-pinned, floor pass only (its determinism comes from the funnel it feeds, not stamps).

**9. Two-lane TDD, enforced.** Test-author agent (spec→red) and implementer agent (green) in separate contexts; implementer's commits must show an empty diff on test files vs the test-author's commit — a checked field at review. Piloted on the ledger slice itself; promote into `tdd` if it holds.

**10. `workflow-debug` diagnose-first becomes mechanical.** `stamp diagnose` requires `repro_cmd` with captured non-zero exit (checked) + attested root cause; `stamp fix` requires the same command exiting 0 (checked) + regression test present. Irreproducible bugs use the audited override.

**11. Merge `workflow-build-one` + `workflow-debug` → `workflow-deliver`.** One orchestrator; ledger step-template differs by `kind` (`bug` inserts required `diagnose`). The "never route bugs to build-one" cardinal rule moves from routing (fragile) to the ledger (a `kind: bug` run cannot reach `stamp fix` without a red repro) — misroutes stop being fatal. `execute-prd`/`run-backlog`/`execute-phase` (batch/phase drivers) and `workflow-feature` (pre-delivery) stay separate.

**12. Three-layer corpus architecture, lint-enforced.** `layer:` frontmatter — **kernel** (deterministic scripts+tests, no LLM judgment), **orchestrator** (thin sequencers; gates are kernel calls; no inline bash procedures), **judgment** (reviews/councils/planning; determinism via evals + report contracts, never stamps). `lint-skill-suite.sh` enforces layer rules.

**13. Review deep pass.** Profile floor computed from diff stats + per-repo path patterns; model may escalate, never run below floor (`stamp review` fails if chosen < computed floor, absent override). Lane files stay in `/tmp` but `stamp review` ingests them: existence + `verdict:` line verified; per-lane verdict/sha256/linecount/`model_used` digests recorded in `state.yaml`; stamp fails if a lane's model is below the profile floor (closes C29; active transcript cross-check explicitly deferred). TDD decision (`ran|not_applicable_with_reason`) becomes a ledger field, warn-level at review stamp initially.

**14. Retirements now, overlap consolidations deferred.** The 6 self-declared-superseded stubs (`pr-responder`, `pr-review`, `review`, `slop-cleaner`, `v1-idea-grill`, `rowan`) are formally retired this effort after an invocation-log check. AUDIT_REPORT overlap groups (decision-log/-memo; repo-audit/improve-codebase-architecture/deep-dive-review) go to skill-backlog as separate grills — bolting judgment-skill consolidation onto an enforcement effort bloats both.

**15. Scoreboard** (reported by `skill-system-audit`): golden-eval pass rate (≥95%), gate coverage (% merges with valid stamps in opted-in repos, target 100%), override rate (~0–2/mo with real reasons; a spike means fix the gate, not the metric), corpus lint (zero dangling/arrow-only refs in orchestrators, layer rules clean). Placement: lints in pre-commit; ledger tests always in CI; golden evals path-filtered in CI.

**16. Bootstrap exception, explicit and expiring.** Phase 0 (kernel) is reviewed under today's prose `workflow-review`, noted in the PR. Every Phase 1+ PR must carry ledger stamps — the system reviews its own rollout. Exception expires when Phase 0 merges.

**17. Backlog reconciliation.** Before Phase 1: ground-truth in-flight PRs #104–#113 (`gh pr list`) — router/finalize are shared files. On landing: flip resolved skill-backlog rows (C22 fully; C19, C29, C20/C32 parts) with ground-truth citations, so the next harvest doesn't re-propose what this effort built.

**Alternatives considered & rejected** (per-question detail in grill transcript 2026-08-18): router-first rollout (nothing to write evidence into); pathspec-aware freshness (judgment in a script costume); hard-block-only override (bricks delivery on script bugs); routes.yaml rules engine (deterministically wrong); uniform deep pass on all chain skills (wayfinder churn, build-one already migrated); full corpus reorg now (half-migrated-corpus risk — the 2026-06-21 restore incident); committing lane review files to the repo (PR noise; digests give ~90% of audit value); HMAC-signed stamps (hook-blocking direct edits is simpler; crypto is a later add-on if bypasses appear).

**Status.** Design accepted (grill complete, all decisions human-provenance). Implementation pending: phase plan → Phase 0 kernel slice. Supersedes-in-part: D-005's caller-migration backlog folds into the chain floor passes; `_docs/step-ledger.md` + `state-cockpit.md` + `human-gate-taxonomy.md` become `workflow-ledger` content when Phase 0 lands.

### D-006 addendum — August-reflection review (2026-08-19, decisions 18–21, human provenance)

Reviewed all 20 unharvested reflections (2026-08-04 → 2026-08-17) against the design; four additions accepted:

**18. Entry enforcement (closes the ad-hoc spiral).** In opted-in repos, Edit/Write to tracked code with no active run in the ledger → hook warns (week 1), then exits 2 with "route via workflow-router or `ledger.sh init`". Evidence: 3MF sessions (2026-08-11) — three ignored reminders to use workflow-debug/tdd/router; exit gates can't catch a session that never enters the system. Rollout per the 2026-08-05 blast-radius lesson: warn-first, opted-in repos only.

**19. Ledger durability.** Live run state moves to `.git/ledger/state.yaml` (survives `reset --hard`/checkout; naturally per-worktree); `stamp` writes a committed snapshot to `docs/executions/state.yaml` for the PR-visible audit record. Evidence: AE-232 (2026-08-17) lost state.yaml edits to a reset. Rejected: auto-commit every write (history noise); tracked-only (proven lossy); untracked-only (no audit).

**20. Forge abstraction.** Checked fields (`ci`, `pr_state`, resolved threads) dispatch through a `forge.sh` shim (origin-detected: gh vs Forgejo API, per the existing `git-forge` pattern); merge-gate hook regex covers `gh pr merge|ready`, `git-forge` merge, `tea pr merge`, and curl `/pulls/*/merge`. Documented honestly: merge shapes outside those patterns bypass the hook — audited-override philosophy (loud where not preventable). Evidence: 3× "Forgejo ≠ GitHub" reflections (2026-08-11/12); GitHub-only enforcement would miss the personal-infra fleet where ad-hoc pressure is highest.

**21. Watch-item + floor riders.** (a) Counter-evidence logged: rowan-soak (2026-08-11) found specialist fan-out added orchestration overhead without decision-quality gain — two-lane TDD pilot (decision 9) keeps its anti-self-approval rationale but collapses to single-lane + empty-test-diff check if the pilot reproduces overhead-without-value. (b) grill-with-docs floor pass adds the pre-lock ground-truth gate (Iceberg reversal 2026-08-06 + Airflow proxy-fix 2026-08-04: in-repo-checkable decisions must be verified before locking/durable fan-out). (c) Phase 5 adds a `skill-backlog` harvest run — the 20 August reflections are unharvested; this effort's cluster consumption (C19/C22/C29/C20/C32) and the August material must land in the ledger, not bypass it.

### D-006 track decisions (2026-08-19, Q32–Q36, human provenance)

**Track B (router measurement).** Golden set: ~40 cases at `workflow-router/references/golden-routes.yaml` (~25 harvested from `skill-invocations.log`, ~15 synthetic adversarial on known confusions); a regression corpus — every future misroute adds a case. Runner `test/routing-eval.sh`: `claude -p` headless, sonnet pinned, deterministic string-match on the ROUTE_CARD `Selected flow` line — no LLM judge. Pass ≥95%. CI: workflow_dispatch + path-filtered PR trigger; non-blocking two weeks, then required.

**Track C (floor mechanics).** Layer tagging staged: tag only touched skills now; lint warn-mode for untagged, error-mode for tagged-but-violating; fleet-wide sweep only after the taxonomy survives ~20 skills. Arrow-ref conversion: one cheap-agent PR, long-tail only; agent classifies invocation-arrows vs map-arrows and escalates ambiguous cases rather than converting them; `lint-skill-refs.sh` must pass after.

**Track D (hygiene).** Retirements: delete the 6 stub dirs outright (git history is the tombstone), atomic catalog updates (router section, skills-index, SKILL-MANIFEST, AUDIT_REPORT) + lint + post-merge Codex mirror; precondition per the 2026-06-21 corrected audit rule: grep `skill-invocations.log`, any invocation in 60 days → escalate that stub to Alex instead of deleting. stderr-suppression hook moved to Track A (already in the Phase 0 spec). August-reflection harvest runs via `skill-backlog` as its own lane.

### D-006 eval substrate (2026-08-19, human provenance)

Pre/post evals adopted: Track B's first real eval run = the router baseline; a read-only gate-compliance transcript audit (last ~40 merged PRs) = the gate baseline, captured before any enforcement merges; `skill-evaluator` blind A/B reserved for Phase 3's review/finalize rewrites only. **`~/projects/audition` adopted as the A/B eval substrate** for Phase 3 and the two-lane-TDD watch-item (D-006 addendum #21a) — its verdict discipline (median >10% AND disjoint ranges, else inconclusive) and `already_tested` ledger dedup replace hand-rolled comparisons. Prerequisite queued in the audition repo (outside dotdev): a `claude -p` launch adapter (audition currently launches `pi` only — per the target-runtime rule, pi-runtime A/Bs are a proxy for Claude-harness skills) plus an optional per-run content-assertion hook (e.g. "output contains a well-formed gate block"). Quality judge stays deferred per audition's own rule. Not used for Track B's router eval (per-case exact-match, not A/B).

### D-006 relay addendum — handoff relay runner (2026-08-19; approval: Alex directive 2026-08-19 (session); drafted with this change, ratified at merge)

**Decision.** Automate the "session ends with a handoff → human manually starts the next session" loop with `workflow-ledger/scripts/relay.sh`: a kernel-adjacent runner that chains headless legs (`claude -p "<resume prompt>" --output-format stream-json --verbose`, transcripts per leg in the workdir) through one handoff file until the work is genuinely done or a human gate stops it.

**Why this is safe to automate now: because of D-006.** Every relayed leg runs under the same hooks, ledger gates, and stamps as an interactive session, so the discipline comes from the kernel rather than from supervision. Before D-006 this loop would have been an honor system with nobody watching.

**Gate-honoring stops, err toward stopping.** CONTINUE only on AFK-eligible exit_reasons (`completion-with-follow-ups`, `halt-for-continuation`). STOP on: `exit_reason: complete` or the live ledger reaching `status: done` during the relay (exit 0; a pre-existing `done` at launch is stale and ignored — the check re-arms after each leg); any human-gate signal in the handoff (`NEEDS_HUMAN`/`needs-human` or the three AFK-blocking gate types from D-006 #2's taxonomy, or a `blocker:` line), plus a missing/unparseable/off-whitelist exit_reason (exit 2); no-progress via handoff sha256 (3); max-legs (4); stop-file kill switch (5); nonzero leg exit, which outranks even a `complete` handoff (6).

**Tripwire, not a sandbox (honest framing, adopted from the R1 security lane).** The stop conditions detect what *cooperating* legs declare in the handoff and the ledger; a leg that can lie in the ledger can lie in the handoff. The enforcement boundary remains the hooks and gate stamps inside each leg — the relay adds no authority and grants no auto-merge authority (the leg prompt denies merge, and states nothing written during the relay can grant it). Wayfinder HITL ticket types must not be relayed; their handoffs should carry NEEDS_HUMAN (not auto-emitted today — the handoff writer must include it).

**Consequences.** `handoff`'s `exit_reason` vocabulary is now one canonical machine-readable set (`complete` / `completion-with-follow-ups` / `halt-for-continuation` / `needs-human`), replacing the older prose list; the handoff document template carries an explicit `exit_reason:` line. Tested by `test/test-relay.sh` (red-first, PATH-stubbed `claude`, zero real API calls). Reviewed under the full four-lane profile across two rounds.

### D-006 #4 refinement — content-verified snapshot exemption (2026-08-18, review R1; ratified by Alex 2026-08-18, along with the R1/R2-mandated test-lane commits per D-006 #9)

Strict-SHA freshness as written was unimplementable (a stamp's own snapshot commit would stale it) and the red suite's subject-based exemption was forgeable (R1 MF1: any commit titled `chore(ledger):` stayed fresh). Refinement: freshness fails on any commit after the stamp **unless the commit touches only `docs/executions/state.yaml`, verified by `git diff-tree` contents**. Subjects are never consulted. Same content rule applies in `reconcile` drift detection. Companion R1 must-fix closures: model floors derive from the computed profile (attested values may only escalate; family-substring ranking incl. fable); finalize resolves its PR via forge `pr-for-branch` itself (`no_pr` only on a successful empty lookup; attested pr_number must match); `FORGE_MOCK_DIR` refused in stamps without the test sentinel; preflight parses parentheticals/`;`/`or` segments and reports unparsed prose instead of dropping it; Edit/Write hook matcher registered in settings; merge-gate blocks only on kernel verdicts (1/2) and warn-permits on kernel breakage per D-006 #5.

### D-006 Phase 1 — router hardening landed (2026-08-18, agent provenance; PR #161)

First fully ledger-stamped delivery (D-006 #16 bootstrap exception expired). `workflow-router/SKILL.md` only; golden set untouched per the recorded rule. Golden eval: **90% baseline (36/40) → 100% (40/40)**; the 4 baseline failures (2, 21, 24, 35) fixed by routing-rule wording: `direct` is now an ALL-of predicate + "inline-doable is not a routing signal" counter-rule (cases 2/35 cited in-rule); UX-copy wording changes classified as tracked code → `workflow-feature` (case 21); imperative verb+skill-name now beats the owner-default carve-out (case 24). Two PR #156 routing gaps closed (`clarity-review` prose carve-out, standalone `grill-with-docs` row → SB-128/129 flipped implemented). Kernel wiring per D-006 #12: resume-check → `ledger.sh reconcile`, preflight → `ledger.sh preflight`, state persistence → `ledger.sh init`/`set` (hand-written state.yaml prose removed — hook-blocked since Phase 0); `layer: orchestrator` tagged (Track C). Mid-iteration eval regressions (case 26 consistent, case 39 flaky) were fixed by sharpening the sql-review-vs-dbt-project-evaluator and approved-V1-brief wording — eval-set never edited. `docs/executions/ci-commands.yaml` created as the repo's verify-local manifest.

### D-006 Phase 2 — workflow-deliver merge landed (2026-08-18, agent provenance)

Decision 11 implemented: `workflow-build-one` + `workflow-debug` merged into one `workflow-deliver` orchestrator (`layer: orchestrator`, per decision 12: thin sequencer, gates are kernel calls, explicit `Load and run` refs only). Kind selection (`bug|feature|skill|docs`) comes from the router; `ledger.sh init --kind bug` inserts required `diagnose`/`fix` steps and the kernel refuses `stamp fix` without a red repro — the old prose cardinal rule ("never route bugs to build-one") is now two sentences plus a kernel reference, and a misroute is corrected by re-initing, not fatal. The two source skills became tombstone redirects (`disable-model-invocation: true`; directories kept because run-backlog/execute-prd/execute-phase/workflow-autonomous-backlog and long-tail docs still reference them — Phase 4/5 rewires callers, then deletes). Router rows (bug, ready-issue, single-issue, default-flow diagram) now target `workflow-deliver`, with two legacy-name compatibility rows keeping `routing-eval.sh --dry-run` green. **Golden set deliberately untouched**: cases 11 (`workflow-build-one`) and 18–19 (`workflow-debug`) will fail the full eval until the separate golden-set migration PR flips their `expected_route` to `workflow-deliver` — that PR must follow immediately. Kernel absorbed the enforcement prose: merged SKILL.md is materially shorter than the two sources combined (duplicated invariants deleted per D-005/D-006 drift rule). `lint-skill-suite.sh` `needs_ledger` exempts the two tombstones until Phase 4/5 deletes them, with a companion check that fails if either name is revived without `disable-model-invocation`. Deliberate drops from the sources, recorded per review round 1: workflow-debug's `reasoning: high` frontmatter (deliver pins sonnet/medium per the Phase 2 spec; review lanes carry their own model floors) and its implement-approach table routing crash/perf/config bugs to `execute-phase` profiles (deliver defaults bug fixes to `tdd`; `execute-phase` stays a phase driver, not a bug-fix backend). Mid-run kind correction is documented as `init --kind bug --force` (audited force-init, agent-initiated allowed for kind correction) and the TDD decision must ride the implement step's completion evidence — both prose-vs-kernel contradictions caught by the Phase 2 review.

### D-006 Phase 3 — review/finalize rewired onto the kernel (2026-08-18, agent provenance; PR #169)

Decisions 12/13 implemented for the review/finalize pair. `workflow-review` (108→96 lines, `layer: judgment`): profile selection defers to `ledger.sh review-floor` (floor computed from diff stats + `docs/executions/review-patterns.txt`; judgment may escalate above, never run below — an under-floor review is unrecordable), the trigger-table prose restatement deleted with the lane table kept as the floor's lane documentation, freshness prose deleted (kernel owns strict-SHA freshness), and the lane-file contract stated once: canonical keys `integrated`/`logic`/`tests`/`security`/`style`, **run-scoped paths via `--attest lanes=`** (the kernel's bare `/tmp/<lane>-review.md` default is cross-run bleed — a stale approve at that path passes every stamp check, observed live in the Phase 3 review), mandatory `model:`/`verdict:`/`reviewed_sha:` header lines now also mandated by the reviewer-briefs Shared Output Contract, orchestrator checks `reviewed_sha` vs HEAD before stamping. **The judgment/stamp seam resolved per #12**: workflow-review *produces* lane files + a synthesis verdict and never stamps; the *invoking orchestrator* records the gate — `workflow-deliver` Step 4 states the same seam and carries the stamp flags, including `model_floor=` escalation (the kernel's model floor tracks the *computed* floor, not the chosen profile). `workflow-finalize` (277→198 lines, `layer: orchestrator`): precondition is one kernel call (`ledger.sh check review`), three freshness restatements collapsed to kernel references, verification is `ledger.sh verify-local`, the gate block reframed as the render of `ledger.sh stamp finalize` whose forge checks own PR-state ground truth; judgment kept (describe-pr discipline, reviewer fan-out, repo-delivery-policy + CODEOWNERS grounding, cleanup coupling). **Known collision recorded (Phase 3 review F3)**: the finalize stamp requires `pr-state = open` while `human-only` keeps PRs draft — Step 8 documents the routes (non-draft with user approval, or audited override on explicit instruction); kernel `draft` acceptance queued as follow-up together with the R2 should-fixes (spaced verdict tokens in 11 untouched briefs; follow-up citation wording). Dropped gate-block fields verified consumer-free; retained fields keep watch-ci/run-backlog/execute-prd/skill-system-audit contracts intact. **Audition's first production A/B** (per the D-006 eval-substrate decision): old vs new workflow-review SKILL.md as `--append-system-prompt` variants on the claude launcher, fixed review-scenario prompt, n=3, metric cost, verdict-line content assertion — verdict verbatim: `adopt B-new-review-skill (cost lower by 10%)` (delta 0.102, ranges disjoint, 6/6 `content_ok`; old median $0.993, new $0.891). Caveat: the claude launcher lives on audition's `feat/claude-launcher` branch, not main as the phase brief assumed.

### D-006 Phase 3 F3 follow-up resolved — draft-PR finalize acceptance re-landed on main (2026-08-19, agent provenance)

The kernel `draft` acceptance queued as follow-up in the Phase 3 entry shipped in PR #170 (`ec61612`) but never reached main: #170 merged into stacked parent `feat/phase3-review-finalize` *after* that parent had already landed via #169, so the fix was unreachable from `origin/main` and every stamped run since (#171, #176) worked around the finalize↔draft deadlock via a scratch `git worktree add --detach ec61612` checkout. Re-landed by cherry-pick onto a fresh worktree from `origin/main` under a kind=bug ledger run: the fix is byte-identical (`checked_finalize` accepts `pr-state` `open|draft`, records the actual state; any other *reported* state still refuses — a real merged/closed PR usually resolves to `forge=no_pr` at the open-PR lookup instead, which stamps successfully with the forge checks skipped), the red test was adapted to the batch-#8 sanitized flat mock names (the original nested mock path would have fake-passed via the leftover `none` lookup), and the verdict-token docs siblings came along. Review round 2 added merged-refusal coverage, full-message assert needles, mock-state restoration after the block, and corrected the finalize SKILL.md merged/closed claim to match the lookup semantics. The Phase 3 "Known collision" note in `workflow-finalize/SKILL.md` Step 8 is replaced by the draft-stamps-normally paragraph.

### Corpus consolidation batch 2 (2026-08-19, Alex-approved per-item; agent execution)

Five items, one PR. Evidence base: `~/.claude/logs/skill-invocations.log` full span (2026-07-20 → 2026-08-19) — zero invocations ever of `herdr`, `herdr-launch`, `humanizer-exec`, `stage-v1-concept`, `find-skills`, `decision-memo`; `humanizer` 8; `deep-research` 1. **(1) herdr-launch → herdr:** the pair was 673 lines with zero invocations; merged `herdr` slimmed aggressively (382+291 → single file under 250 lines) with herdr-launch's genuinely distinct content — stage-appropriate companion-tool launches (implement/review/ci/cleanup) — kept as one section. herdr-launch is a #164-pattern tombstone (redirect body, `disable-model-invocation: true`). **Retirement candidate if still zero-use at next audit** — the herdr auto-naming extension covers the automatic case. **(2) humanizer-exec → humanizer exec mode:** more than tone constraints (exec-register pattern table, sharpen-don't-reorganize scope rule, no-fabricated-headline gate), so it landed as a full § Exec mode section, not a paragraph; humanizer-exec tombstoned; sole skill caller `workflow-executive-doc` rewired (flow diagram, Step 9 polish now `Load and run humanizer` in exec mode, pairs-well list). **(3) Twin disambiguation, no retirement** (plugin twins aren't ours to remove): corpus `find-skills` description claims canonical status over `meta-skills:find-skills`; corpus `deep-research` description names the engine boundary vs `meta-skills:deep-research` (Claude-native AFK web research with cited markdown asset vs OpenAI Deep Research API — choose by engine). **(4) Exec-writing seam:** `decision-memo` ↔ `workflow-executive-doc` mirror sentences (single decision → exec one-pager vs full memos/board docs); exec-doc's decision sections now explicitly compose `decision-memo` where they reduce to a single recommendation. **(5) stage-v1-concept → v1-workflow Step 2.25 (Stage the Concept):** scratch/ephemeral grill promotion (pending context/decision/ADR entries → repo docs, name/slug, location + git-init human gates, restart brief) absorbed concisely; stage-v1-concept tombstoned; `grill-with-docs` promotion handoff rewired to the new step. **Explicitly examined and KEPT per Alex's per-item answers:** the retro trio and `graph-first` — no change to either. Catalog updates: router Knowledge/utility row 8→6 (herdr-launch's Library/infra row left untouched — the in-flight planning-lane PR owns broader router edits; tombstone dir still resolves refs), skills-index regenerated, SKILL-MANIFEST tombstone annotations, AUDIT_REPORT batch-2 resolution table. `lint-skill-refs` green post-rewire.

### D-006 planning-lane consolidation — design-plan folded into to-prd, execute-phase retired (2026-08-19, human provenance; Alex-approved)

**Decision.** Collapse the two parallel planning formalisms into one: `design-plan` becomes a **migration mode inside `to-prd`** and `execute-phase` is retired outright. The refactor/migration lane is now `repo-audit` findings → `to-prd` (migration mode) → `to-issues` → `triage` → `execute-prd`. Both retired skills are #164-pattern tombstone redirects (`disable-model-invocation: true`, short body naming the successor + date + this decision); directories — including `execute-phase/references/` — stay in place until a later sweep rewires long-tail callers.

**Evidence.** Over the full `skill-invocations.log` span (begins 2026-07-20; re-greped 2026-08-19): `design-plan` 1 invocation (2026-07-20), `execute-phase` 0, `execute-prd` 1 (2026-07-31). Post-#168 all slices route through `workflow-deliver`, so a standalone phase runner and a phased-plan-file formalism duplicate the PRD + issue-tree formalism — two parallel planning vocabularies was the muddiness removed. Corrected-audit-rule preconditions met: invocation history, replacement coverage (below), explicit Alex approval 2026-08-19, rollback = revert the tombstones.

**What migrated (discipline, not prose — per #12).** to-prd migration mode absorbs design-plan's genuinely distinct value as PRD deltas: `FIND-NN`/`REQ-NN` anchors preserved verbatim from audit findings; phased sequencing expressed as parent issue + ordered children with explicit dependencies (executed by `execute-prd`, never a separate phase runner); a pilot/canary first slice with canary-before-deletion; per-child rollback expectations; sync gates as explicit human-marked child issues. Mode is selected by input shape (repo-audit report / migration brief vs product brief); product mode is untouched.

**execute-prd retained — different altitude.** It orchestrates dependency-ordered issue trees *above* the `workflow-deliver` unit loop (ordering, waves, reconciliation, parent handoff), which no other skill owns. A `run-backlog` merge (independent-batch vs dependent-tree drivers) is deferred pending boundary-confusion evidence.

**Mechanics.** Router: refactor/migration rows now target `to-prd` (migration mode) with two legacy-name tombstone rows (`design-plan`, `execute-phase`) mirroring the D-006 #11 pattern; Specialized Audit/Refactor Lane, Audit Loop Retirement, and Roadmap Gate Rule (in-rule migration-mode exception — the human-approved audit/brief satisfies the gate; not a waiver) sections rewritten. `lint-skill-suite.sh`: `execute-phase` moved to the tombstone `needs_ledger` exemption; both names added to the revival check, which now asserts `disable-model-invocation: true` by *value* (a `false` no longer passes — review R1 security finding) and is covered by `test/test-skill-suite-lint.sh` assertions. Golden set untouched (eval/router separation rule) — greped 2026-08-19: **zero** existing `expected_route: design-plan|execute-phase` cases to migrate; the paired eval PR that must follow adds ~3 redirect-row cases ("turn this audit into a phased plan" → to-prd; "create a refactor plan" → to-prd; "execute phase N" → execute-prd) so the new legacy-name rows are actually exercised (review R1 tests finding). Caller rewires landed in the same PR (repo-audit + synthesizer, wayfinder funnel, workflow-roadmap, workflow-finalize post-mortem gate, to-issues incl. anchor-preservation rule, prompt-builder, omc-reference, user-journey-qa, reconcile-issues, setup-worktree core-loop diagram + identity surfaces, watch-ci delivery-loop diagram, describe-pr artifact map marked legacy, `dotfiles/.claude/reference/workflows.md` route + tombstone bullets — PR #179 merged mid-run, unblocking it — and pairs-with mentions). Deferred to the later sweep: `describe-pr` Step 0's plan/phase-evidence *classification behavior* (migration-mode/multi-phase work must reach `issue_only` or an issue-tree-backed mode instead of demanding `.phase-runs/` — a behavior fix, not a wording fix), plus a `lint-skill-refs.sh` extension flagging active-skill references to tombstoned names in `references/**` and `/slash` forms.

### D-006 Phase 5b — route evidence + prompt-time routing reminder (2026-08-19, agent provenance; PR #182)

Routing enforcement made structural at three layers, motivated by a live incident: a session produced a delivery plan with no ROUTE_CARD, misrouted to `design-plan` against that skill's own contract, and skipped the prior-art gate — caught only by human review. **Layer 1 (prompt-time):** a `UserPromptSubmit` hook in `dotfiles/.claude/settings.json` emits one deterministic reminder line into every prompt's context (`[router] Delivery-shaped request (build/fix/change tracked code)? Invoke workflow-router first — ROUTE_CARD before dispatch.`) — no conditionals, deterministic emission by design. **Layer 3 (kernel):** `ledger.sh init --route "<classification>|<selected-flow>|confirmed"` records a top-level `route:` field (schema addition; malformed → exit 6 at init and on every load). Absent route: init succeeds with `WARNING: no route evidence — invoke workflow-router`; `LEDGER_REQUIRE_ROUTE=block` escalates to exit 11 — the same warn-then-flip pattern as entry enforcement. `workflow-router`'s ledger-persist step passes `--route` from the confirmed ROUTE_CARD. **Layer 4 (scoreboard):** `skill-system-audit`'s gate-coverage recipe also reports route-evidence coverage (% of merged runs whose snapshot carries `route:`). Red-first: 22 red assertions (round-trip, warning, block env, malformed shapes, tampered-state load); dogfooded on this run's own ledger (audited force-init re-init with route evidence).

**Flip preconditions for `LEDGER_REQUIRE_ROUTE=block`** (recorded here per the Phase 5a convention — 5a's entry-enforcement preconditions live in ci.yml comments + PR #175's body; this flag has no CI surface, so the log is its home):

1. **Route-evidence coverage soaks first.** Do not flip until `skill-system-audit` shows route coverage at/near 100% across a full audit window of merged runs — a flip while orchestrators still init route-less bricks delivery at init time, the exact failure mode D-006 #5 rejects.
2. **All init call sites pass `--route`.** `workflow-router` does as of this PR; `workflow-deliver`, `execute-prd`, `run-backlog`, `execute-phase`, and any AFK dispatcher that calls `ledger.sh init` directly must thread route evidence through before the flip.
3. **AFK route provenance decided.** A batch dispatcher has no human-confirmed ROUTE_CARD per child run; decide what an AFK init records (e.g. the dispatching workflow's own classification with the parent run's confirmation) before the env is set anywhere AFK runs execute.
4. **Flip mechanism is machine-global env** (`settings.json` `env` block), mirroring `LEDGER_ENTRY_ENFORCE` — never a per-session export, which would make enforcement session-lottery.

### D-006 per-run committed snapshots — end cross-PR state.yaml conflicts (2026-08-19, agent provenance, user-directed design)

The single committed snapshot path was a structural defect: every run's `init`/`stamp`/`close` committed `docs/executions/state.yaml`, so ANY two concurrent stamped PRs merge-conflicted on it — observed as #167 (twice), #174, #180, #181 stalling on exactly this within 24h, each costing a keep-ours resolution plus a full restamp ceremony. Fix: the committed snapshot is now per-run at `docs/executions/runs/<run_id>.yaml`; concurrent runs never commit a shared path. **Pointer replacement weighed and rejected**: a tracked `state.yaml` pointer rewritten by every `init` re-creates the same cross-PR conflict (both branches rewrite the pointer line since merge-base); locally the live state already carries `run_id`, and in CI the PR diff names the run file — so the shared path is dropped entirely. `docs/executions/state.yaml` stays as a frozen historical record (never written/read by new runs; satisfies no gate). Consumers updated: freshness exemption covers exactly the run's OWN file (a commit touching another run's file is STALE); `check-snapshot` gains `--file` plus live-run/single-file resolution; `finalize-stamp-check.sh` discovers candidates from the PR diff (≥1 fresh passes — a superseded force-re-init sibling may be stale; zero candidates on a non-exempt diff fails); guard rule 2 blocks `docs/executions/runs/*.y(a)ml`; handoff/skill-system-audit/router/finalize/state-cockpit docs repointed. run_ids are now filenames, so `init` refuses path-hostile shapes (exit 6). Folded in (same kernel PR): `worktree-baseline.sh cut` absolutizes a relative `--path` — two agents had hand-patched sidecars after relative-path cuts (forged-baseline-adjacent; #171 made verify recompute ground truth, cut now just does it). Red-first: 66 red assertions across 4 suites, including the design-goal test — two runs in two worktrees merge into the same base with no conflict. **Review-round hardening (full profile, 5 lanes, all REQUEST_CHANGES → fixed red-first, +16 assertions):** nested `runs/` paths are refusable everywhere the kernel cannot author them (guard any-depth, CI candidate filter, `--file` shape validation with physical-path canonicalization + tracked-file requirement); `run_id` charset allowlisted (it is a git pathspec in `commit_snapshot`); `init` refuses reusing a committed run_id without `--force`; the unresolvable-base CI fallback enumerates HEAD run files instead of the kernel's forever-AMBIGUOUS single-file tier; schema-invalid siblings stay loud on PASS; the audit recipe's legacy fallback is conditioned on pre-migration heads (unconditional, it scored unstamped post-migration PRs as "stamped"). **Migration consequence (documented in `docs/executions/runs/README.md`):** runs inited pre-upgrade lose stamp freshness (their legacy snapshot commits are no longer exempt) and must re-stamp under the new kernel — deliberate; a legacy exemption would undo the tamper tightening.

### D-006 multi-repo adoption — external snapshot root, work repos stay pristine (2026-08-19, agent provenance, user-directed design; ratified at merge)

**Problem, measured.** A cross-repo audit of the 14 local git projects (13 under `~/projects` plus `dotdev`, 2026-08-19) found enforcement kernel-shaped in `dotdev` only, and the opt-in signal actively misleading elsewhere. `workflow-guard.sh:248` treats any repo containing `docs/executions/` as opted in, then blocks the merge on `ledger.sh check finalize` exit 1 or 2 (`workflow-guard.sh:251-270`); `LEDGER_SH` resolves to an absolute path that exists on this machine, so the block really can fire in any repo. **Seven** repos carry that directory without kernel-shaped state — `chorus`, `delphi`, `bookmarks`, `pergamon` with a pre-kernel hand-written `state.yaml` (no `stamps:` key), and `alexandria`, `circuit`, `insta-scrape` with none — and all seven return `exit=1 MISSING: no live ledger state`, verified by running the kernel read-only in each. So "opted in" meant "the next merge from a Claude session hard-blocks, with zero checked gate evidence to show for it." Meanwhile four repos with live delivery sat in one of the two broken states — three of them shared-remote ClassDojo repos, plus chorus on a local Forgejo remote — and the ungated ones are the busiest. Counts are commits on each repo's **remote-tracking default branch** (`origin/HEAD`), after a fetch, in the window opening 2026-08-12 17:00 EDT — the figure that means "code reaching the gated branch": `taro` **30 on `origin/staging`** and `ml-models` **20 on `origin/main`** (of which, since 2026-08-18 00:00 EDT, **18 commits with 16 landing directly on the branch**, only 2 through a PR) lack the directory entirely and so ran with **zero gates**; `chorus` (13) and `delphi` (10) have it and therefore sit in the **hard-block** state described above — chorus on a local Forgejo remote rather than a ClassDojo one, which is why it is counted among the seven mis-shaped repos and not among the shared-remote three. No merge *commits* landed in `chorus` or `delphi` after the hook went live; a blocked merge leaves no commit behind, so "nobody has hit the block yet" is plausible but not provable from git.

Three corrections to the figures as merged in PR #193, all found by review after the fact. The merged paragraph published `taro` "21 commits/7d, on `main`", `ml-models` 20, `delphi` 22, and no figure for `chorus`. First, taro's figure was wrong twice over: 21 was an all-refs count, and taro's gated branch is `origin/staging`, not `main` — the correct figure for the branch its code actually lands on is **30**, while `main` (which the merged text named) reads 2. Second, delphi's 22 was likewise all-refs; `origin/main` reads 10. Third, chorus had no figure at all and belongs in the comparison at 13 — it earns its place as one of the seven mis-shaped opted-in repos, not as a ClassDojo repo, since its remote is a local Forgejo instance.

The merged paragraph's closing claim — that no merge commits had landed in chorus or delphi since the hook went live — **stands, and was re-verified rather than assumed**: measured from the hook's go-live date (2026-08-18) rather than from this metric's activity window, both read zero. An earlier draft of this correction withdrew that sentence on the strength of four chorus merges, but those all predate the hook and fall inside the activity window only; comparing a claim against the wrong boundary is the same error this whole correction is about, committed while correcting it.

So the corrections **strengthen** this entry's conclusion rather than reverse it. taro was already named first among the zero-gate repos; corrected, taro (30) and ml-models (20) are the two busiest of the three shared-remote ClassDojo repos — the third, delphi, reads 10 — and taro and ml-models are exactly the two with no gates at all. `dotdev` runs hotter still at 38 and chorus at 13, which is why a comparison like this has to name its set out loud. What changed is magnitude and rigour, not direction. `skill-system-audit` step 5 now pins the method — remote-tracking ref, `origin` fetch, absolute window, `origin/HEAD` refreshed rather than assumed — because an unstated activity metric silently chooses an audit's priorities. The mislabelled figure itself was catchable by re-measurement (`main` reads 2 against a published 21, a 10x miss) and that is how review found it; what re-measurement could not catch was the *provenance*. Every figure here was derived independently more than once and confirmed each time; the false claims about which document those figures came from survived all of it, and were caught only by a lane briefed to read the prior text instead of re-measuring — three separate times, on this one paragraph. Verifying a claim about a text means opening the text, and checking a claim's scope means using the boundary the claim named.

**Decision.** Adopt the kernel across active repos, but **never write ledger artifacts into a repo that is not `dotdev`**. A run in an external repo keeps live state where it already lives (`.git/ledger/state.yaml`, per-worktree) and commits its snapshot to an **external snapshot root inside dotdev**: `docs/executions/external/<repo-name>/<run_id>.yaml`. The work repo's tracked tree, its `.gitignore`, and its history stay untouched; the audit trail stays durable and reviewable in the repo that owns the D-006 system.

This supersedes the "local-only enforcement, `.git/ledger` only" shape chosen earlier the same day (Alex, 2026-08-19, in-session and never logged — this entry is its first and only record): that variant kept work repos clean at the cost of losing the record the moment the branch was gone, and it made work-repo gate coverage unmeasurable by the scoreboard, which reads committed snapshots. Committing to dotdev instead keeps both properties. The adoption-scope answer stands as given — work repos get enforcement without artifacts; the change is only *where* the artifact goes.

**What this requires (none of it shipped by this entry — this is the record, not the implementation).**

1. **Snapshot root indirection in the kernel.** `set_snapshot_rel` (`ledger.sh:105-106`) resolves the snapshot under the *current* repo's `$TOP`, and `stamp` commits it there (`ledger.sh:667`/`669`, `git -C "$TOP" add`/`commit`). External-root mode needs the write target and the `git -C` target to be the dotdev checkout while `$TOP` stays the work repo. Freshness gets *simpler*, not harder: with no snapshot commits in the work repo, the content-verified snapshot-only exemption in `fresh_since` (D-006 #4, `decision-log.md:156` — exempt only when the whole changed-path set equals `$SNAPSHOT_REL`) has nothing to exempt, so any commit after a stamp is stale, full stop.

2. **A repo allowlist as the sole opt-in signal, from the mode's first commit.** Directory presence cannot be the signal, because `docs/executions/` is not a ledger tree — it is the shared execution-record tree, and `handoff/SKILL.md:17,63,81` writes `<repo-root>/docs/executions/handoffs/<date>-<slug>.md` and commits it with the branch **in every repo**, with four other skills (`cleanup-delivery`, `execute-prd`, `prompt-builder`, `run-backlog`) and `_docs/state-cockpit.md` referencing the same path. That is how all seven repos acquired the directory. So the stale opt-in is not a legacy artifact from an earlier era; it is a live recurring one that the next `/handoff` recreates. The signal becomes an explicit list committed in dotdev (proposed: `docs/executions/external/repos.txt`, one absolute path or remote slug per line). Explicit beats inferred, and directory presence must stop being consulted at all — leaving it as a fallback re-arms the trap on the next handoff.

3. **Guard write-protection must cover the external tree.** The block regex at `workflow-guard.sh:187` matches `docs/executions/state.yaml`, `docs/executions/runs/*.yaml`, and `.git/**/ledger/state.yaml`. An external snapshot at `docs/executions/external/<repo>/<run_id>.yaml` matches none of them — confirmed by probe — so hand-edits would be permitted, the exact hole the rule exists to close. Extend the regex with the external path shape in the same change that adds the mode, never after.

4. **Scoreboard reads two roots.** The gate-coverage loop in `skill-system-audit/SKILL.md:107-140` walks merged PR heads in one repo and reads snapshots off `pull/$pr/head`. For external runs the snapshot is not on the work-repo head at all, so the recipe needs a second lookup: for each merged work-repo PR, find `docs/executions/external/<repo>/<run_id>.yaml` in dotdev and check its stamps. Until that exists, work-repo coverage is unmeasured — report it as such rather than as 0%, which would read as a discipline failure instead of a missing instrument.

5. **Migration of the seven mis-shaped repos — the legacy state file only, never the directory.** `docs/executions/` holds tracked non-ledger documentation in most of them (measured: `pergamon` 38 tracked files — 25 phase/issue records + 12 handoffs, `delphi` 7, `bookmarks` 3, `circuit` 2, `insta-scrape` 1, `alexandria` 0; `chorus`'s directory is gitignored and untracked entirely). Removing the directory would delete 51 tracked files, 48 of them not ledger state. So migration is: copy each legacy `state.yaml` under the external root as a historical record, `git rm` **that one file** where it is tracked, add the repo to the allowlist, and leave every handoff and phase record where it is. `alexandria`, `circuit`, and `insta-scrape` have no state file to migrate and need only the allowlist line. For `chorus`, "archive" means copying a file that was never in that repo's history, so nothing is removed there at all. The legacy files are worth archiving rather than deleting: `chorus`'s carries `classification:` and `selected_flow:` — route evidence hand-written on 2026-08-17, two days before Phase 5b (`83aecd4`, PR #182) formalized `route:`, which is evidence the discipline was already there and only the verification was missing.

**Not decided here.** Whether `dotdev` itself keeps its in-repo `docs/executions/runs/` tree (it should — it is the system's own repo, and per-run snapshots just landed in PR #191), and whether the external root should key on remote slug rather than directory name for repos cloned twice. Both are implementation choices for the chip that follows.
