---
name: rowan
description: "DEPRECATED — use brain-ops. Retained as a redirect stub."
disable-model-invocation: true
---

# rowan (retired)

Retired 2026-07-20: the standalone rowan project was tombstoned
(`~/projects/chorus-archive/standalone-rowan-tombstoned-2026-07-20`) and its
`brain` binary path no longer exists, so this skill would fail on invocation.

Use `brain-ops` instead — it owns the brain route over `~/Documents/Home/_brain/`
via the live CLI (`cd ~/Documents/Home/_brain/scripts && uv run brain <cmd>`).

## Contract

Consumes: nothing — retired skill
Produces: a redirect to `brain-ops`
Requires: none
Side effects: none
Human gates: none
