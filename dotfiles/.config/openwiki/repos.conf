# openwiki-scheduled repos — one absolute path per line. ~ is expanded.
# Blank/empty = the scheduler is a no-op. Add openwiki-enabled repos here.
# Example:
# ~/code/my-project
~/dotdev
~/projects/agents/iris
