onboarding = false
[ui.toast]
delivery = "terminal"   # native desktop notifications via Ghostty (agent finished / needs input)

[ui]
show_agent_labels_on_pane_borders = true

agent_panel_scope = "all"
[experimental]
pane_history = true
