## Summary

Closes the three coupled follow-ups from the D-006 Phase 3 review (PR #169) — stacked on `feat/phase3-review-finalize` and intended to merge after it:

- **F3 (bug, kind=bug run):** `checked_finalize` in `workflow-ledger/scripts/ledger.sh` required forge `pr-state = open` exactly, while `forge.sh` maps isDraft PRs to `draft` — so the default `human-only` `REPO_DELIVERY_POLICY` path (PR intentionally kept draft) could never stamp finalize. The stamp now accepts `open` or `draft` and keeps recording the actual state in the checked fields; `merged`/`closed`/error still refuse.
- **NEW-1:** the remaining spaced verdict tokens (`REQUEST CHANGES`, `NEEDS HUMAN`) are underscored in 10 per-lane reviewer briefs, one orchestration rule in `reviewer-briefs.md`, and — per review round 1 — the 10 Pi-runtime mirrors under `dotfiles/.pi/agent/agents/`, which were byte-identical to the canonical briefs before this change and are again after it. The deliberate spaced negative example at `reviewer-briefs.md:16` is preserved.
- **NEW-2:** the workflow-finalize Step 8 "Known collision — draft PRs" workaround paragraph is replaced with a short resolved-state note (draft stamps normally; merged/closed still refuse; never mark a human-only PR ready just to change its state). The citation-wording half of NEW-2 had already landed on the parent branch (`b5c4a15`).

## Red/green

Red commit adds the draft-PR coverage to `test/test-ledger.sh` before the fix: exactly 2 assertions fail (draft stamp accepted; `pr_state: draft` recorded), with a `closed`-refusal guard green by design. Fix commit turns the suite green (114 passed / 0 failed). Tests untouched after the red commit. The kernel's own `stamp diagnose` captured the red repro (`bash test/test-ledger.sh`) and `stamp fix` re-ran it green.

## Review

Independent `workflow-review`: integrated lane (opus) R1 **REQUEST_CHANGES** — two verified findings, both fixed: (1) Pi brief mirrors drifted by the canonical token edits (fixed, mirrors byte-identical again); (2) branch stacked on the pre-rebase parent (rebuilt on the current tip, clean cherry-picks). R2 **APPROVE**. The mirror fix pushed the diff past the kernel's 15-file threshold, so the computed floor escalated to `full`: 4 independent opus lanes (security, logic, tests, style) all returned **APPROVE** at the stamped HEAD, findings non-blocking (see Follow-ups). Review + fix + diagnose gates are kernel-stamped in `docs/executions/state.yaml`.

## Test plan

- `bash test/test-ledger.sh` → Passed: 114 / Failed: 0 (includes the new draft-accepted, state-recorded, and closed-refused assertions)
- `lint-skill-refs.sh` → 0 dangling; `lint-skill-suite.sh` → failures=0
- Tests lane independently re-ran the suite and mutation-tested the predicate (a widened `open|draft|closed` predicate fails 2 assertions, as intended)
- This PR's own finalize stamp is the live proof: it was recorded against this draft PR by the fixed kernel

## Issues

None discovered — no tracked issue references this work; it originates from PR #169's review round 2 findings (F3/NEW-1/NEW-2). Disposition: not applicable.

## Follow-ups (review findings, non-blocking by all four lanes)

1. **`merged` assertion gap** (tests + style): the new test block's comment says "merged/closed still refuse" but only `closed` is asserted; a mutation widening the predicate to include `merged` passes the suite. Add the sibling `merged` fixture + 2 asserts (or narrow the comment).
2. **Vacuous `ci=green` on empty GitHub check rollup** (security + logic, convergent): `gh_ci_status` maps zero checks to `green`; draft-gated CI configs now reach a successful stamp with no checks run. Map empty rollup to `pending` (matching the Forgejo backend) or document that `verify-local` is the substantive gate there.
3. **`gh_pr_state` orders `isDraft` before `state`** (security + logic): a closed draft normalizes to `draft`; unreachable today only because `pr-for-branch` filters to open PRs. Defense-in-depth: report closed/merged ahead of draft.
4. **No unit test for the draft normalization** (tests): `forge.sh`'s `isDraft → draft` mapping is now load-bearing but only exercised via mocks; add a gh-shim case to `test/test-forge.sh`.
5. **`reviewer-briefs.md:16` overclaims kernel enforcement** (security): the kernel greps `^verdict:` for existence, not token-set validity; either validate the token set in `checked_review` or reword the line.
6. **`AI_ENVIRONMENT.md:173,301`** (style): two Mermaid edge labels still spell `REQUEST CHANGES`; normalize in a follow-up commit (cannot affect lane-file parsing).

Provenance: body generated by describe-pr (issue_only mode; git log/diff + issue discovery run; graphify not_available_with_reason: no graphify-out/graph.json).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
